# redos-scanner grep coverage test fixtures
# Each non-comment line must be detected by at least one grep pattern

if (/^(a+)+$/.test(userInput)) { ... }
const match = input.match(/^([a-z]+)*$/)
const re = new RegExp('^(' + userPattern + ')+$')
if re.match(r'^(a+)+$', user_input):
result = re.search(r'(x+x+)+y', data)
emails = re.findall(r'([a-z]+@[a-z]+)+', text)
Pattern p = Pattern.compile("^(a+)+$")
# Note: String.matches/String.replaceAll 패턴은 리터럴 "String" 클래스만 매칭.
# 변수명(input)으로 호출하는 케이스는 Phase 1 Source-first에서 발견.
if (String.matches("^(a+)+$")) { ... }
String.replaceAll("(.*)+", "")
if user_input =~ /^(a+)+$/
tokens = input.scan(/([a-z]+)+/)
re = Regexp.new("^(" + user_pattern + ")+$")
val regex = "^(a+)+$".toRegex()
const cleaned = input.replace(/^(a+)+$/, '')
@RequestParam String pattern
@RequestBody SearchDto dto
const search = req.query.q
const filter = req.body.regex
