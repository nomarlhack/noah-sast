# pdf-generation-scanner grep coverage test fixtures
# Each non-comment line must be detected by at least one grep pattern

const pdf = require('html-pdf')  // wkhtmltopdf wrapper
const browser = await puppeteer.launch()
from weasyprint import HTML; HTML(string=user_html).write_pdf('out.pdf')  // WeasyPrint
const doc = new PDFKit()  // PDFKit
pdf = Prawn::Document.new  // prawn
const htmlPdf = require('html-pdf')
const pdfCreator = require('pdf-creator-node')
const printer = new pdfmake(fonts)  // pdfmake
const html2pdf = require('html-pdf-node')  // html.*pdf
const pdfGenerator = createPdfFromHtml(template)  // pdf.*generat
const page = await browser.newPage(); await page.pdf({ path: 'out.pdf' })  // playwright.*pdf
