# xslt-injection-scanner grep coverage test fixtures
# Each non-comment line must be detected by at least one grep pattern

TransformerFactory tf = TransformerFactory.newInstance()
const xsltProcessor = new XSLTProcessor()
from lxml.etree import XSLT; transform = XSLT(xslt_doc)
Nokogiri::XSLT(stylesheet_string)
const xslt = require('xslt-processor')
XslCompiledTransform xslt = new XslCompiledTransform()
$proc->registerPHPFunctions()  // registerPHPFunctions
XsltSettings settings = new XsltSettings(true, true)  // XsltSettings
net.sf.saxon.TransformerFactoryImpl  // saxon
org.apache.xalan.processor.TransformerFactoryImpl  // Xalan
<xsl:stylesheet version="1.0" xmlns:xsl="http://www.w3.org/1999/XSL/Transform">  // XSLT
xslt_string = '<xsl:template match="/">' + user_input + '</xsl:template>'  // xslt
