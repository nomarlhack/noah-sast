# deserialization-scanner grep coverage test fixtures
# Each non-comment line must be detected by at least one grep pattern

data = pickle.loads(request.body)
obj = pickle.load(open(uploaded_file, 'rb'))
config = yaml.load(user_input, Loader=yaml.FullLoader)
obj = Marshal.load(cookie_data)
$obj = unserialize($_COOKIE['session']);
ObjectInputStream ois = new ObjectInputStream(new ByteArrayInputStream(data));
Object obj = ois.readObject();
XMLDecoder decoder = new XMLDecoder(new ByteArrayInputStream(xmlData));
# Note: XStream.fromXML 패턴은 클래스명 직접 호출만 매칭. 변수 호출은 Phase 1 래퍼 추적.
XStream.fromXML(request.getParameter("data"));
BinaryFormatter bf = new BinaryFormatter(); bf.Deserialize(stream);
const serialize = require('node-serialize'); serialize.unserialize(cookie);
const obj = cryo.parse(userInput)
data = jsonpickle.decode(request.data)
$data = maybe_unserialize($value);
db = shelve.open(user_path)
mapper.activateDefaultTyping(ptv, ObjectMapper.DefaultTyping.NON_FINAL)
mapper.enableDefaultTyping()
@JsonTypeInfo(use = JsonTypeInfo.Id.CLASS)
new GenericJackson2JsonRedisSerializer()
JSON.parseObject(jsonStr, Feature.SupportNonPublicField)  // fastjson
com.alibaba.fastjson.JSON.parseObject(data)
SnakeYaml yaml = new SnakeYaml();
Yaml yaml = new Yaml(); yaml.load(input);
BinaryFormatter.Deserialize(networkStream)
JsonConvert.DeserializeObject(json, new JsonSerializerSettings { TypeNameHandling = TypeNameHandling.All })
# phpggc Laravel/RCE1
