# saml-scanner grep coverage test fixtures
# Each non-comment line must be detected by at least one grep pattern

const passport = require('passport-saml')
const { Strategy } = require('@node-saml/passport-saml')  // saml
provider :saml, idp_sso_target_url: 'https://idp.example.com'  // omniauth-saml
require 'ruby-saml'
const samlResponse = req.body.SAMLResponse
<ds:Signature xmlns:ds="http://www.w3.org/2000/09/xmldsig#">
assertionConsumerServiceURL = '/auth/saml/callback'  // ACS
SAML_SETTINGS = { ... }  # SAML config
from onelogin.saml2.auth import OneLogin_Saml2_Auth
auth = OneLogin_Saml2_Auth(req, old_settings)  # python-saml
