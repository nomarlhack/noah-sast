# soapaction-spoofing-scanner grep coverage test fixtures
# Each non-comment line must be detected by at least one grep pattern

headers = { 'SOAPAction': 'urn:getBalance' }
@WebService(targetNamespace = "http://example.com/banking")
@WebMethod(operationName = "transferFunds")
<dependency>org.apache.cxf</dependency>  // Apache CXF
<dependency>org.apache.axis2</dependency>  // Apache Axis
@Endpoint
@PayloadRoot(namespace = "http://example.com", localPart = "TransferRequest")
client = zeep.Client('http://example.com/service?wsdl')
app = Application([BankingService], 'urn:banking')  // spyne
$server = new SoapServer('service.wsdl')
<definitions xmlns="http://schemas.xmlsoap.org/wsdl/">  // wsdl
<?xml version="1.0"?><wsdl:definitions>  // WSDL
client = Savon.client(wsdl: 'http://example.com/service?wsdl')  // savon
<soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">  // soap
$client = new SoapClient($wsdl_url);  // SOAP
